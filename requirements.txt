# API Server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Web Crawling & Extraction
httpx>=0.27.0
beautifulsoup4>=4.12.3
markdownify>=0.12.1
curl-cffi>=0.15.0
# trafilatura and readability-lxml are intentionally omitted.
# The extractor gracefully falls back through trafilatura -> readability -> BeautifulSoup + html.parser.

# LLM / Research
openai>=1.35.0

# Database
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
